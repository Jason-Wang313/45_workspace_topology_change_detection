# Novelty Decision

Decision: pending deep read of hostile prior work.

Interim thesis candidates:
1. Topology-change detection as cached-plan invalidation for robot navigation and manipulation.
2. Workspace structural event detection from incremental map/graph evidence with explicit plan dependency tracing.
3. A topology-aware world-state change detector that targets plan cache invalidation instead of generic map differencing.

Current best guess: candidate 3, but only if prior work does not already subsume it.
