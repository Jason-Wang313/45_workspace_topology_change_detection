# Submission Readiness Decision

Decision: workshop-only.

Reason: the mechanism is clear, but the evidence is synthetic and relies on hand-defined dependency sets. V2 stress shows performance degrades when dependencies are missed or hallucinated.

Next useful research move: extract topology dependencies from an actual planner and test invalidation under noisy SLAM/map updates.
