# Plan

Final v3 plan executed.

1. Replace the small exact-dependency diagnostic with a broad deterministic topology-invalidation benchmark.
2. Treat dependency extraction as an empirical variable rather than assuming oracle plan dependencies.
3. Evaluate strong baselines: raw delta, occupancy IoU, path touch, inflated path touch, connectivity-only, graph-edit distance, centrality change, semantic adjacency, incremental edge update, learned surrogate, and plan-topology dependency.
4. Report safety-specificity metrics: precision, recall, F1, unsafe false negatives, unnecessary invalidation, cache-retention precision, replan cost, topology recall, calibration, and submission score.
5. Rewrite the manuscript to 25 pages with main results, stress tests, ablations, negative controls, split generalization, failure analysis, and extended appendices.
6. Export only `C:/Users/wangz/Downloads/45.pdf`, visually QA it, update docs, commit, and push before moving to Paper46.
